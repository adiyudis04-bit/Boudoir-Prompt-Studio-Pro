/* Tailwind CSS is included via CDN, ensuring modern responsive styling and dark mode compatibility. */
/* No additional custom CSS needed as all styling is handled by Tailwind utility classes in the HTML structure. */
